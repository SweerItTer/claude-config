# Chinese Mirror Disabled

This mirror is no longer part of the default routing path.

- Use `rules/README.md` and the English leaf docs under `rules/common/` and `rules/<language>/`.
- If a Chinese reading aid is explicitly needed, translate from the English source of truth on demand.
- Do not treat this file as an active routing entry.
